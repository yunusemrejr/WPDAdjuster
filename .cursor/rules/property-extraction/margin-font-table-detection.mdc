---
description:Detect defined margins in cm/mm.

Gather font families and font size distributions.

Count tables and check if embedded text is consistent with global styles.

Detect shapes and captions (if supported).
globs:
alwaysApply: true
---
